apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-runner-egress
  namespace: codevault-exec
spec:
  podSelector:
    matchLabels:
      role: runner
  policyTypes:
  - Ingress
  - Egress
  # Empty Ingress and Egress means deny all inbound and outbound traffic
